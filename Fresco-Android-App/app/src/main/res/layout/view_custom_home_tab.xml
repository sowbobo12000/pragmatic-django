<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?android:attr/selectableItemBackground"
                tools:background="@color/accent"
                tools:layout_height="48dp">

    <TextView
        android:id="@+id/tab_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textAllCaps="true"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="HIGHLIGHTS"/>

    <ImageView
        android:id="@+id/notification_image_view"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_alignBottom="@+id/tab_text_view"
        android:layout_alignEnd="@+id/tab_text_view"
        android:layout_alignRight="@+id/tab_text_view"
        android:layout_marginEnd="-4dp"
        android:layout_marginRight="-4dp"
        android:src="@drawable/home_tab_notification"
        android:visibility="gone"/>

</RelativeLayout>